{
  "name": "tilled-node",
  "version": "0.0.2",
  "description": "NodeJS SDK client for Tilled's API",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/gettilled/tilled-node.git"
  },
  "main": "dist/api.js",
  "types": "dist/api.d.ts",
  "scripts": {
    "clean": "rm -Rf node_modules/ *.js",
    "build": "tsc",
    "test": "npm run build && node dist/client.js",
    "generate": "openapi-generator generate -i https://api.tilled.com/docs/spec3.json -g typescript-node -c ./codegen.conf.json --git-repo-id tilled-sdks --git-user-id gettilled --skip-validate-spec --enable-post-process-file && npx prettier --write '**/*.ts' && npm install && npm version patch --force"
  },
  "author": "Tilled",
  "license": "Unlicense",
  "dependencies": {
    "bluebird": "^3.5.0",
    "request": "^2.81.0",
    "rewire": "^6.0.0"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.33",
    "@types/node": "^12",
    "@types/request": "^2.48.8",
    "typescript": "^4.0"
  }
}
